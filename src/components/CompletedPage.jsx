{/*import TaskList from "./TaskListItems";

const CompletedPage = ({ tasks }) => {
  // Filter tasks based on status
  const todoTasks = tasks.filter((task) => task.status === "completed");

  return (
    <div className="container mx-auto">
      <h1 className="text-2xl font-bold text-purple-900 mt-8 mb-4">
        Todo Tasks
      </h1>
      {/* Render TaskList only if there are todo tasks */}
     /* {todoTasks.length > 0 && <TaskList tasks={todoTasks} />}
    </div>
  );
};

export default CompletedPage;*/
